node_modules
.next
out
.DS_Store
*.tsbuildinfo
# All env files with secrets — NEVER commit these
.env.local
.env.vercel
.env*.local
.env
# But DO allow the example file (no real secrets)
!.env.example
.agents